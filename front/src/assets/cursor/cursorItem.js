import common from "./common.png";
import hover from "./hover.png";
import forbidden from "./forbidden.png";
import input from "./input.png";

// 원하는 형식으로 가져다 쓰세요.
export { common, hover, forbidden, input };

const cursor = {
  common,
  hover,
  forbidden,
  input,
};

export default cursor;
